git checkout dev
git pull origin dev

git checkout -b pr
git push -u origin pr

git checkout dev
git checkout -b sit
git push -u origin sit

git checkout dev
git checkout -b uat
git push -u origin uat

git checkout dev
git checkout -b prod
git push -u origin prod

git checkout dev
